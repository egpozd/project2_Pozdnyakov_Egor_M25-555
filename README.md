# Primitive Database

![Python Version](https://img.shields.io/badge/python-3.9%2B-blue)
![License](https://img.shields.io/badge/license-MIT-green)

Простая консольная база данных на Python с полной поддержкой CRUD-операций и современными возможностями Python. Проект демонстрирует применение продвинутых концепций Python: декораторы, замыкания, обработка исключений, работа с файлами и пакетирование.

---

## 🎬 Демонстрация

Посмотрите демонстрацию работы всех функций базы данных, выполненных в соответствии с частью 3 ТЗ

[![asciicast](https://asciinema.org/a/0D6zKWmoHuScLwMi956ZBcBwI.svg)](https://asciinema.org/a/0D6zKWmoHuScLwMi956ZBcBwI)

Посмотрите демонстрацию работы всех функций базы данных, включая новые возможности с декораторами

[![asciicast](https://asciinema.org/a/Vgx6S2cz9AU3knywz78ZzDcQP.svg)](https://asciinema.org/a/Vgx6S2cz9AU3knywz78ZzDcQP)

---

## ✨ Особенности

- **Полный CRUD**: Создание, чтение, обновление, удаление записей
- **Управление таблицами**: Создание, удаление, просмотр таблиц
- **Декораторы**: Обработка ошибок, подтверждение действий, замер времени
- **Кэширование**: Ускорение повторных запросов
- **Валидация типов**: Поддержка `int`, `str`, `bool`
- **Красивый вывод**: Форматированные таблицы с PrettyTable
- **Персистентность**: Автоматическое сохранение в JSON-файлы
- **Безопасность**: Подтверждение опасных операций

---

## 📦 Установка

### Для пользователей

```bash
pipx install primitive-db
```

После установки приложение доступно командой:

```bash
primitive-db
```

### Для разработчиков

#### Предварительные требования

- Python 3.9+
- Poetry
- Git

#### Пошаговая установка

1. **Клонируйте репозиторий:**

```bash
git clone <repository-url>
cd project2_Pozdnyakov_Egor_M25-555
```

2. **Установите зависимости:**

```bash
make install
```

3. **Проверьте качество кода:**

```bash
make lint
```

4. **Соберите пакет:**

```bash
make build
```

#### Запуск в режиме разработки

**Способ 1: Через Makefile**

```bash
make project
```

**Способ 2: Через Poetry**

```bash
poetry run project
```

**Способ 3: Прямой запуск модуля**

```bash
poetry run python -m src.primitive_db.main
```

#### Другие полезные команды

```bash
make publish          # Тест публикации пакета
make package-install  # Установка собранного пакета
```

---

## 🚀 Быстрый старт

```bash
primitive-db
```

### Основные команды

```sql
create_table users name:str age:int is_active:bool
insert into users values ("Иван", 25, true)
select from users
update users set age = 26 where name = "Иван"
delete from users where is_active = false
drop_table users
```

---

## 📚 Полная документация

### Управление таблицами

| Команда | Описание |
|---------|----------|
| `create_table <имя> <столбцы>` | Создать таблицу |
| `list_tables` | Показать все таблицы |
| `drop_table <имя>` | Удалить таблицу (с подтверждением) |
| `info <имя>` | Информация о таблице |

### Операции с данными

| Команда | Описание |
|---------|----------|
| `insert into <таблица> values (<значения>)` | Добавить запись |
| `select from <таблица> [where <условие>]` | Выбрать записи |
| `update <таблица> set <столбец=значение> [where <условие>]` | Обновить записи |
| `delete from <таблица> [where <условие>]` | Удалить записи (с подтверждением) |

### Общие команды

| Команда | Описание |
|---------|----------|
| `help` | Справка |
| `exit` | Выход |

---

## 📖 Пример работы

```
Введите команду: create_table products title:str price:int
Таблица "products" успешно создана

Введите команду: insert into products values ("Телефон", 25000)
Функция insert выполнилась за 0.015 секунд
Запись с ID=1 успешно добавлена

Введите команду: select from products
Функция select выполнилась за 0.008 секунд
+----+---------+-------+
| ID |  title  | price |
+----+---------+-------+
| 1  | Телефон | 25000 |
+----+---------+-------+

Введите команду: drop_table products
Вы уверены, что хотите выполнить "удаление таблицы" для таблицы "products"? [y/N]: y
Таблица "products" успешно удалена
```

---

## 🏗️ Структура проекта

```
project2_Pozdnyakov_Egor_M25-555/
├── src/primitive_db/
│   ├── core.py          # Основная логика базы данных
│   ├── engine.py        # Интерфейс командной строки
│   ├── decorators.py    # Декораторы для улучшения кода
│   ├── parser.py        # Парсеры сложных команд
│   ├── utils.py         # Вспомогательные функции
│   └── main.py          # Точка входа
├── data/                # Директория для файлов данных
├── Makefile             # Автоматизация задач
├── pyproject.toml       # Конфигурация Poetry
└── README.md            # Документация
```

---

## 🔧 Технические детали

### Используемые технологии

- **Poetry**: Управление зависимостями и пакетирование
- **Ruff**: Статический анализ и форматирование кода
- **PrettyTable**: Красивый вывод таблиц
- **Prompt**: Удобный ввод пользовательских данных

### Архитектурные особенности

- **Декораторы**: `@handle_db_errors`, `@confirm_action`, `@log_time`
- **Замыкания**: Кэширование результатов запросов
- **Модульность**: Четкое разделение ответственности между компонентами
- **Обработка ошибок**: Централизованная система обработки исключений

### Хранение данных

- **Метаданные**: Файл `db_meta.json`
- **Данные таблиц**: Отдельные JSON-файлы в директории `data/`
- **Автоматическое создание**: Структура создается при первом использовании

---

## 📝 О проекте

Primitive Database — учебный проект, демонстрирующий современные возможности Python и лучшие практики разработки.

---

